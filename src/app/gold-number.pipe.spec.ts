import { GoldNumberPipe } from './gold-number.pipe';

describe('GoldNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new GoldNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
