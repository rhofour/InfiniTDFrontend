import { ClickShortcutDirective } from './click-shortcut.directive';

describe('ClickShortcutDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickShortcutDirective();
    expect(directive).toBeTruthy();
  });
});
